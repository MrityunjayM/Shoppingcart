<%- include("./layouts/header");-%>


    <div class="container my-3">
        <% page.forEach((page)=>{%>
            <% if(page.slug=="home" ){%>
                <%=page.content%>
                    <% } %>
                        <% }) %>
                        
    </div>
    <%- include("./layouts/footer"); -%>